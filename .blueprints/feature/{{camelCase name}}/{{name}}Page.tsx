import {
  use{{name}}Actions,
  use{{name}}State
} from './{{name}}Module';

export function {{name}}Page() {
  const { } = use{{name}}Actions();
  const {} = use{{name}}State();
  return <div></div>
}
