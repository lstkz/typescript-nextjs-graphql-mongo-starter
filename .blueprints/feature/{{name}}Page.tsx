import {
  use{{name}}Actions,
  use{{name}}State,
  {{name}}Module
} from './{{name}}Module';

function {{name}}Page() {
  const { } = use{{name}}Actions();
  const {} = use{{name}}State();
  return <div></div>
}
